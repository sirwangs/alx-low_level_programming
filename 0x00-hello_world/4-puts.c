#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print a string to output
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char Tbw[] = "\"Programming is like building a multilingual puzzle";

	puts(Tbw);

	return (0);
}
