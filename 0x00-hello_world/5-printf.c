#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the program prints a string to output'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
